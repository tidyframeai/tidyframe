# TidyFrame Frontend Environment Configuration
# Copy this file to .env.local for local development
# CRITICAL: Never commit .env.local with real values to version control

# =============================================================================
# API CONFIGURATION
# =============================================================================
# Backend API URL (used for proxy during development and API calls)
# Development: http://localhost:8000
# Production: https://api.tidyframe.com or your backend URL
VITE_API_URL=http://localhost:8000
VITE_BACKEND_URL=http://localhost:8000

# =============================================================================
# CONTACT INFORMATION
# =============================================================================
# Admin/Contact email displayed in frontend
VITE_ADMIN_EMAIL=tidyframeai@gmail.com

# =============================================================================
# ENVIRONMENT
# =============================================================================
NODE_ENV=development  # Options: development, production

# =============================================================================
# NOTES
# =============================================================================
# - Vite automatically loads .env.local for local development
# - Variables must be prefixed with VITE_ to be exposed to the client
# - In production, the frontend is built as static files and served by the backend
# - The backend URL is proxied through /api in development mode
# - No secrets should be stored in frontend .env files (they're exposed to the browser)
